spring.application.name=holy-matrimony-backend

server.port=8080

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/holymatrimony
spring.datasource.username=hm_user
spring.datasource.password=Hm@2026Secure
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Flyway (disable for now)
spring.flyway.enabled=false

# JWT
jwt.secret=HolyMatrimonySecureSecretKey2026HolyMatrimonySecureSecretKey
jwt.expiration=86400000
# Debug Logging
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
server.error.include-message=always
server.error.include-stacktrace=always
server.error.include-binding-errors=always